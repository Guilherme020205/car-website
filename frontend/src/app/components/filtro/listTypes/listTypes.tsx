import { useState } from 'react';

export function ListTypes() {
  const [selected, setSelected] = useState(''); // Inicializa com null para nenhum selecionado

  return (
    <div className="flex flex-row gap-1 items-center">
     
    </div>
  );
}
